import React from 'react'
import { useAxiosState } from '../hooks/axiosHooks'

function SelectedPokemon (props){
    const { pokemon } = props
    // const [pokeDetails, setPokeDetails] = useAxiosState(pokemon.species.url)

    // if (!pokeDetails){
    //     return null
    // }

    // console.log(pokeDetails)

    return null
}

export default SelectedPokemon